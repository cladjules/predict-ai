{
  "name": "market-admin",
  "version": "1.0.0",
  "scripts": {
    "simulate": "cd .. && cre workflow simulate market-admin -T staging-settings --broadcast",
    "simulate:prod": "cd .. && cre workflow simulate market-admin -T production-settings --broadcast",
    "deploy": "cd .. && cre workflow deploy market-admin -T staging-settings",
    "deploy:prod": "cd .. && cre workflow deploy market-admin -T production-settings"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.1.1"
  }
}
